/**
 * Data hooks for POS can be extracted from pages/POS.jsx as that module grows.
 * Currently the POS page owns its own state; this file reserves the feature boundary.
 */
export function usePosDataPlaceholder() {
  return null
}
